name: Third Workflow
on: [push]

jobs:
  run-my-command:
    runs-on: ubuntu-latest
    steps:
      - name: Copy Repo
        run : |
          pwd
          ls -a
      - uses: actions/checkout@v4
      - name: Copy Repo
        run : ls -a

